# 刷题网页生成 — 可复用指令

> 下次做同类产品时，把这条指令 + 题库发给我即可。

---

## 指令

```
我要做一个刷题网页，要求：

1. 题库在"作业库"文件夹中（.doc 格式）
2. 一键运行 python 一键生成刷题网页.py 完成：
   - .doc → .txt 转换
   - 题目解析（单选/多选/判断/计算题）
   - 处理内联选项（A.xxx B.xxx 在同一行需拆分）
   - 如有答案文件，自动匹配答案
   - 生成 200 枚激活码
   - 生成带激活锁的 quiz-app.html
3. 刷题页面必须包含以下功能：
   - 按章节划分板块刷题
   - 每次刷完题记录历史成绩（localStorage），首页可查看历次记录
   - 刷题时可 ☆ 收藏题目，首页可查看/跳转收藏列表
   - 托管地址自动检测（在线自动获取，本地兜底填入线上 URL）
   - 首页「📱 生成二维码」按钮，生成整页二维码（扫码进入首页看到全部章节，非单章二维码）
4. **页面风格统一使用暖色调设计**：
   - 背景色 `#f5f0eb`（暖米色）、卡片色 `#fffbf7`（暖白）、强调色 `#b8860b`（暗金色）
   - 移动优先响应式，max-width 680px，大圆角卡片布局（12px/10px）
   - 字体：PingFang SC / Microsoft YaHei
   - 交互：单选题点击选项即判（正确自动前进）、多选题多选后确认判、判断题双按钮
   - 功能标配：激活锁 + 章节列表 + 收藏☆按钮 + 刷题历史记录 + 错题重做
5. 输出 quiz-app.html 上传 GitHub Pages
6. 输出激活码列表（.md + .txt）
7. 网页标题：[填入标题]
8. 激活码数量：[填入数量，默认 200]
```

---

## 使用示例

```
我要做一个"中级财务会计"的刷题网页，题库是"D:\题库\中级财务会计"文件夹
里的 15 个 .doc 文件。网页标题叫"中级财务会计 · 章节练习"，生成 300 个激活码，
其他都一样。
```

---

## 运行命令

```bash
python 一键生成刷题网页.py
```

一键完成全部 5 个步骤：

| 步骤 | 说明 |
|------|------|
| 1 | .doc → .txt（调用 Word COM） |
| 2 | 解析题目 → JSON |
| 3 | 生成激活码 + SHA-256 哈希 |
| 4 | 生成 quiz-app.html |
| 5 | 生成激活码列表 |

---

## 部署到线上

```bash
# 将 quiz-app.html 上传到已有 GitHub 仓库
git add quiz-app.html
git commit -m "更新题库"
git push
```

GitHub Pages 自动部署，30 秒生效。

如果是全新项目，先去 GitHub 创建仓库，然后：
```bash
git init
git add quiz-app.html
git commit -m "刷题网页上线"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

再到仓库 Settings → Pages → Source: main → Save。

---

## 修改激活码数量

编辑 `一键生成刷题网页.py` 第 31 行：
```python
ACTIVATION_COUNT = 500  # 改为需要的数量
```
